import {Room} from './Room';

export class Booking{
    id?: number;
    roomName?: string;
    memberEmail?: string;
    roomID: number;
    from: string;
    to: string;
    description?: string;
}
